<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MouseKeeper.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="MouseKeeper">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="32,22,32,26">
        <Grid.Resources>
            <Storyboard x:Name="PulseStoryboard" RepeatBehavior="Forever">
                <DoubleAnimation
                    Storyboard.TargetName="PulseScale"
                    Storyboard.TargetProperty="ScaleX"
                    From="1.0" To="1.18" Duration="0:0:1.6">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <DoubleAnimation
                    Storyboard.TargetName="PulseScale"
                    Storyboard.TargetProperty="ScaleY"
                    From="1.0" To="1.18" Duration="0:0:1.6">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <DoubleAnimation
                    Storyboard.TargetName="PulseRing"
                    Storyboard.TargetProperty="Opacity"
                    From="0.55" To="0.0" Duration="0:0:1.6">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid>
            <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                <Border
                    Width="32" Height="32"
                    CornerRadius="16"
                    Background="{ThemeResource AccentFillColorDefaultBrush}">
                    <FontIcon
                        Glyph="&#xE962;"
                        FontSize="14"
                        Foreground="White" />
                </Border>
                <TextBlock
                    Text="MouseKeeper"
                    FontSize="18"
                    FontWeight="SemiBold"
                    VerticalAlignment="Center" />
            </StackPanel>

            <Border
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Padding="10,5"
                CornerRadius="12"
                Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                BorderBrush="{ThemeResource SubtleFillColorTertiaryBrush}"
                BorderThickness="1">
                <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <Ellipse
                        Width="8" Height="8"
                        Fill="{x:Bind StatusBrush, Mode=OneWay}" />
                    <TextBlock
                        Text="{x:Bind StatusBadge, Mode=OneWay}"
                        FontSize="12"
                        FontWeight="SemiBold" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- Hero Ring -->
        <Grid Grid.Row="1" Margin="0,18,0,18">
            <Grid
                Width="248" Height="248"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

                <!-- Pulse halo (only visible when active) -->
                <Ellipse
                    x:Name="PulseRing"
                    Width="248" Height="248"
                    Stroke="{x:Bind StatusBrush, Mode=OneWay}"
                    StrokeThickness="2"
                    Opacity="0"
                    RenderTransformOrigin="0.5,0.5">
                    <Ellipse.RenderTransform>
                        <ScaleTransform x:Name="PulseScale" ScaleX="1.0" ScaleY="1.0" />
                    </Ellipse.RenderTransform>
                </Ellipse>

                <!-- Track -->
                <Ellipse
                    Width="232" Height="232"
                    Stroke="{ThemeResource SubtleFillColorTertiaryBrush}"
                    StrokeThickness="12" />

                <!-- Progress arc -->
                <Path
                    x:Name="ProgressArc"
                    Stroke="{x:Bind StatusBrush, Mode=OneWay}"
                    StrokeThickness="12"
                    StrokeStartLineCap="Round"
                    StrokeEndLineCap="Round" />

                <!-- Center label -->
                <StackPanel
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Spacing="4">
                    <TextBlock
                        Text="{x:Bind HeroText, Mode=OneWay}"
                        FontSize="46"
                        FontWeight="SemiBold"
                        HorizontalAlignment="Center"
                        TextAlignment="Center"
                        LineHeight="52" />
                    <TextBlock
                        Text="{x:Bind HeroSubtext, Mode=OneWay}"
                        FontSize="13"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        HorizontalAlignment="Center"
                        TextAlignment="Center"
                        TextWrapping="Wrap"
                        MaxWidth="180" />
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Toggle -->
        <Button
            x:Name="ToggleButton"
            Grid.Row="2"
            MinHeight="52"
            HorizontalAlignment="Stretch"
            CornerRadius="26"
            Click="ToggleButton_Click"
            Style="{StaticResource AccentButtonStyle}">
            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                <FontIcon
                    Glyph="{x:Bind ToggleGlyph, Mode=OneWay}"
                    FontSize="14" />
                <TextBlock
                    Text="{x:Bind ToggleButtonText, Mode=OneWay}"
                    FontSize="15"
                    FontWeight="SemiBold" />
            </StackPanel>
        </Button>

        <!-- Settings: idle delay -->
        <Grid Grid.Row="3" Margin="0,20,0,0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Spacing="2" VerticalAlignment="Center">
                <TextBlock
                    Text="Wartezeit"
                    FontSize="13"
                    FontWeight="SemiBold" />
                <TextBlock
                    Text="Pause vor dem sanften Bewegen"
                    FontSize="12"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <controls:SelectorBar
                x:Name="DelaySelector"
                Grid.Column="1"
                SelectionChanged="DelaySelector_SelectionChanged">
                <controls:SelectorBarItem Text="3 s" Tag="3" />
                <controls:SelectorBarItem Text="10 s" Tag="10" />
                <controls:SelectorBarItem Text="30 s" Tag="30" />
                <controls:SelectorBarItem Text="60 s" Tag="60" />
            </controls:SelectorBar>
        </Grid>

        <!-- Footer: shortcut chip + hint -->
        <Grid Grid.Row="4" Margin="0,18,0,0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                VerticalAlignment="Center"
                Text="Bewegst du die Maus, pausiert MouseKeeper sofort."
                FontSize="12"
                TextWrapping="Wrap"
                Foreground="{ThemeResource TextFillColorTertiaryBrush}" />

            <Border
                Grid.Column="1"
                Padding="10,5"
                CornerRadius="10"
                Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                BorderBrush="{ThemeResource SubtleFillColorTertiaryBrush}"
                BorderThickness="1"
                ToolTipService.ToolTip="Globaler Kurzbefehl zum Umschalten">
                <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <FontIcon
                        Glyph="&#xE765;"
                        FontSize="11"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <TextBlock
                        Text="{x:Bind ShortcutText, Mode=OneWay}"
                        FontSize="12"
                        FontWeight="SemiBold" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
